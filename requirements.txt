astroid==2.2.5
certifi==2019.6.16
chardet==3.0.4
idna==2.8
isort==4.3.21
lazy-object-proxy==1.4.1
mccabe==0.6.1
mypy==0.711
mypy-extensions==0.4.1
Pillow==6.1.0
pylint==2.3.1
requests==2.22.0
six==1.12.0
typed-ast==1.4.0
urllib3==1.25.3
vk-api==11.5.0
wrapt==1.11.2

# TODO Не нужно в файл requirements запихивать вообще все библиотеки. Надо только те, которые используются
# в текущем проекте, и без зависимосетей. Потому что если основных пакетов 25, то у них ~100 пакетов в зависимостях.
# И вы/коллега решили обновить не основной пакет - и все падает!
# Или еще хуже - два основных пакета используют разные версии зависимых,
# получается DLL hell https://ru.wikipedia.org/wiki/DLL_hell
# Чтобы минимизировать все это - ТОЛЬКО основные пакеты.
# Поэтому для задания с билетом пусть будет свой файл requirements, а для бота свой.